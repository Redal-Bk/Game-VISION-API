@using System.Security.Claims
@using System.Text
<link href="~/321654987846985749674967.ico" rel="icon" type="image/x-icon" />
<nav class="navbar navbar-expand-lg navbar-dark sticky-top">
    <div class="container">
        <a class="navbar-brand" href="index.html">
            <i class="bi bi-controller"></i> GameVision
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link" href="/Home">خانه</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/Game">بازی‌ها</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/News">اخبار</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" href="/Profile">پروفایل</a>
                </li>
            </ul>
            <div class="d-flex align-items-center gap-3">
                <input type="search" class="search-box" placeholder="جستجوی بازی...">

                @* وضعیت کاربر در هدر *@
                @if (User.Identity?.IsAuthenticated == true)
                {
                    <div class="d-flex align-items-center gap-3">
                        @{
                            string avatarUrl = null; // اول null می‌ذاریم
                            string userName = User.FindFirst(ClaimTypes.Name)?.Value ?? "کاربر";

                            // ۱. اگر با دیسکورد لاگین کرده باشه
                            if (User.FindFirst("urn:discord:id")?.Value is string discordId &&
                            User.FindFirst("urn:discord:avatar")?.Value is string avatarHash)
                            {
                                avatarUrl = $"https://cdn.discordapp.com/avatars/{discordId}/{avatarHash}.png?size=64";
                                userName = User.FindFirst("urn:discord:username")?.Value ?? userName;
                            }
                            // ۲. اگر کاربر معمولی آواتار آپلود کرده باشه
                            else if (User.FindFirst("AvatarUrl")?.Value is string customAvatar && !string.IsNullOrEmpty(customAvatar))
                            {
                                avatarUrl = customAvatar;
                            }

                            // ۳. اگر هیچ آواتاری نداشت → آدمک نئونی گیمینگ (SVG)
                            if (string.IsNullOrEmpty(avatarUrl))
                            {
                                avatarUrl = "data:image/svg+xml;base64," + Convert.ToBase64String(Encoding.UTF8.GetBytes(@"
                                        <svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100' width='64' height='64'>
                                        <defs>
                                        <linearGradient id='neonGrad' x1='0%' y1='0%' x2='100%' y2='100%'>
                                        <stop offset='0%' style='stop-color:#00f0ff' />
                                        <stop offset='100%' style='stop-color:#b200ff' />
                                        </linearGradient>
                                        </defs>
                                        <circle cx='50' cy='50' r='48' fill='none' stroke='url(#neonGrad)' stroke-width='4' opacity='0.8'/>
                                        <circle cx='50' cy='35' r='15' fill='none' stroke='url(#neonGrad)' stroke-width='3'/>
                                        <path d='M30 60 Q50 80 70 60' stroke='url(#neonGrad)' stroke-width='3' fill='none'/>
                                        <circle cx='35' cy='30' r='4' fill='url(#neonGrad)'/>
                                        <circle cx='65' cy='30' r='4' fill='url(#neonGrad)'/>
                                        </svg>
                                        "));
                            }
                        }

                        <img src="@avatarUrl"
                             class="rounded-circle border border-2"
                             style="width: 50px; height: 50px; object-fit: cover; border-color: var(--primary-neon)!important; box-shadow: 0 0 15px rgba(0, 240, 255, 0.4);"
                             alt="آواتار کاربر" />

                        <div class="d-flex flex-column">
                            <span class="fw-bold text-white" style="text-shadow: 0 0 10px var(--primary-neon);">
                                @userName
                            </span>
                            <small class="text-secondary">
                                @User.FindFirst(ClaimTypes.Role)?.Value
                            </small>
                        </div>

                        <a href="/Account/Logout" class="btn btn-sm btn-outline-neon">
                            <i class="bi bi-box-arrow-right"></i> خروج
                        </a>
                    </div>
                }
                else
                {
                    <a href="/Account" class="btn btn-neon px-4">
                        <i class="bi bi-box-arrow-in-right me-2"></i>
                        ورود / ثبت‌نام
                    </a>
                }
            </div>
        </div>
    </div>
</nav>
@RenderBody()
<footer class="footer">
    <div class="container">
        <div class="row">
            <div class="col-md-4 mb-4">
                <h5 class="mb-3">
                    <i class="bi bi-controller"></i> GameVision
                </h5>
                <p class="text-secondary">بزرگترین پلتفرم گیمینگ فارسی با اخبار، بررسی‌ها و اطلاعات کامل بازی‌ها</p>
            </div>
            <div class="col-md-4 mb-4">
                <h6 class="mb-3">لینک‌های سریع</h6>
                <ul class="list-unstyled">
                    <li class="mb-2"><a href="/Home" class="footer-link">خانه</a></li>
                    <li class="mb-2"><a href="/Game" class="footer-link">بازی‌ها</a></li>
                    <li class="mb-2"><a href="/News" class="footer-link">اخبار</a></li>
                    <li class="mb-2"><a href="/Profile" class="footer-link">پروفایل</a></li>
                </ul>
            </div>
            <div class="col-md-4 mb-4">
                <h6 class="mb-3">شبکه‌های اجتماعی</h6>
                <div class="d-flex gap-3">
                    <a href="https://instagram.com/Gamevision" class="footer-link fs-4"><i class="bi bi-instagram"></i></a>
                    <a href="https://t.me/Gamevision" class="footer-link fs-4"><i class="bi bi-telegram"></i></a>
                    <a href="https://google.com" class="footer-link fs-4"><i class="bi bi-discord"></i></a>
                </div>
            </div>
        </div>
        <hr style="border-color: var(--border-color);">
        <div class="text-center text-secondary">
            <p>&copy; 2026 GameVision. تمامی حقوق محفوظ است.</p>
        </div>
    </div>
</footer>